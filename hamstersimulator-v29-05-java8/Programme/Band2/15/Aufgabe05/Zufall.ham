/*class*/// generiert Zufallszahlen
class Zufall {
    // erzeugt Zufallszahlen zwischen 0 und max
    static int randomNum(int min, int max) {
        int random = (int) (Math.random()*(max-min+1)+min);
        return random;
    }
}